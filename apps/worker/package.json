{
  "name": "@belezafoco/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"Worker tests pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@belezafoco/database": "workspace:*",
    "@nestjs/common": "^11.1.16",
    "@nestjs/config": "^4.0.2",
    "bullmq": "^5.58.3",
    "ioredis": "^5.8.1",
    "pino": "^9.9.5"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
